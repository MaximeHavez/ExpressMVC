<!DOCTYPE html>
<html lang="fr">
<head>
    <%-include('./header.ejs') %>
</head>
<body>
<%-include('./navbar.ejs')  %>
<h1 class="text-center mt-3">Utilisateurs</h1>
<div class="d-flex justify-content-center mb-3">
    <a href="/users/createUser" class="btn btn-primary">Ajouter un utlisateur</a>
</div>
<div class="d-flex flex-wrap justify-content-center gap-2">
<% for (let i = 0; i < users.length ; i++) { %>

<div class="card" style="width: 18rem;">
    <img src="<%= users[i].photo %>" class="card-img-top" alt="...">
    <div class="card-body">
        <h5 class="card-title"><%= users[i].name %></h5>
        <a href="/users/display/<%= users[i].id %>" class="btn btn-primary">Détail</a>
        <a href="/users/delete/<%= users[i].id %>" class="btn btn-danger">Supprimer</a>
    </div>
</div>

<% } %>
</div>

</body>
</html>